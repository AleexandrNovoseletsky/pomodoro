# Pomodoro Task Management API

[![Python Version](https://img.shields.io/badge/python-3.12+-blue.svg)](https://www.python.org/downloads/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.118+-green.svg)](https://fastapi.tiangolo.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-17-blue.svg)](https://www.postgresql.org/)
[![Redis](https://img.shields.io/badge/Redis-7+-red.svg)](https://redis.io/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

> **Производственное REST API для управления задачами в технике Pomodoro с аутентификацией, медиа-файлами и кэшированием**

## 📋 Описание

**Pomodoro API** — это высокопроизводительное REST API для управления задачами в технике Pomodoro. Система предоставляет полный набор инструментов для организации работы: создание задач, управление категориями и тегами, загрузка медиа-файлов, аутентификация пользователей и интеграция с внешними сервисами.

### 🎯 Ключевые возможности

- **📊 Управление задачами**: CRUD операции с задачами, категориями и тегами
- **👥 Аутентификация**: JWT-токены, OAuth 2.0 (Yandex), ролевая система
- **📁 Медиа-файлы**: Загрузка изображений с автоматической обработкой (WebP, миниатюры)
- **⚡ Кэширование**: Redis для ускорения запросов и снижения нагрузки на БД
- **🔒 Безопасность**: Rate limiting, валидация данных, защита от уязвимостей
- **📧 Уведомления**: Email-уведомления для восстановления пароля
- **🐳 Контейнеризация**: Docker Compose для локальной разработки
- **📈 Мониторинг**: Структурированное логирование и обработка ошибок

## 🏗️ Архитектура

Проект построен на принципах **чистой архитектуры** с разделением на слои:

```
pomodoro/
├── main.py                 # Точка входа FastAPI приложения
├── core/                   # Общие компоненты
│   ├── exceptions/         # Кастомные исключения
│   ├── handlers/           # Глобальные обработчики
│   ├── repositories/       # Базовые репозитории
│   ├── services/           # Базовые сервисы
│   ├── settings.py         # Конфигурация
│   └── utils/              # Утилиты
├── auth/                   # Аутентификация и авторизация
│   ├── handlers/           # API endpoints аутентификации
│   ├── services/           # Бизнес-логика аутентификации
│   ├── models/             # Модели OAuth аккаунтов
│   └── schemas/            # Pydantic схемы
├── user/                   # Управление пользователями
│   ├── handlers/           # API endpoints пользователей
│   ├── services/           # Сервисы пользователей
│   ├── models/             # Модели пользователей
│   └── schemas/            # Схемы пользователей
├── task/                   # Управление задачами
│   ├── handlers/           # API endpoints задач
│   ├── services/           # Сервисы задач
│   ├── models/             # Модели задач/категорий/тегов
│   ├── repositories/       # Репозитории задач
│   └── schemas/            # Схемы задач
├── media/                  # Управление медиа-файлами
│   ├── handlers/           # API endpoints загрузки
│   ├── services/           # Сервисы обработки файлов
│   ├── models/             # Модели файлов
│   ├── storage/            # S3/MinIO интеграция
│   └── utils/              # Утилиты обработки изображений
└── database/               # Работа с данными
    ├── database.py         # SQLAlchemy конфигурация
    ├── cache/              # Redis интеграция
    └── accessor.py         # Доступ к БД
```

### 🏛️ Принципы архитектуры

- **SOLID**: Принципы объектно-ориентированного дизайна
- **DRY**: Избегание дублирования кода
- **Dependency Injection**: Внедрение зависимостей через FastAPI Depends
- **Repository Pattern**: Абстракция доступа к данным
- **Service Layer**: Бизнес-логика отделена от контроллеров
- **Clean Architecture**: Независимость от фреймворков

## 🚀 Быстрый старт

### 📋 Предварительные требования

- **Python 3.12+**
- **Poetry** для управления зависимостями
- **Docker & Docker Compose** для инфраструктуры
- **Git** для клонирования репозитория

### ⚡ Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/AleexandrNovoseletsky/pomodoro.git
   cd pomodoro
   ```

2. **Установите зависимости:**
   ```bash
   poetry install
   ```

3. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env  # Создайте файл с переменными окружения
   ```

   Заполните `.env` файл необходимыми значениями (см. раздел [Конфигурация](#конфигурация)).

4. **Запустите инфраструктуру:**
   ```bash
   docker-compose up -d
   ```

5. **Выполните миграции базы данных:**
   ```bash
   make migrate
   ```

6. **Создайте суперпользователя:**
   ```bash
   make root
   ```

7. **Запустите приложение:**
   ```bash
   make run
   # Или напрямую:
   # poetry run uvicorn pomodoro.main:app --reload --host 127.0.0.1 --port 8000
   ```

8. **Проверьте работу:**
   Откройте [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs) для интерактивной документации API.

### 🧪 Запуск тестов

```bash
# E2E smoke тесты (требуется запущенное приложение)
make e2e

# Линтинг и форматирование
make ruff
```

## 📚 API Документация

### 🔐 Аутентификация

API использует **JWT Bearer токены** для аутентификации. Получите токен через `/auth/login` и передавайте его в заголовке:

```
Authorization: Bearer <your_jwt_token>
```

#### Ролевая система

- **USER**: Обычный пользователь (доступ к своим ресурсам)
- **ADMIN**: Администратор (доступ ко всем ресурсам пользователей)
- **ROOT**: Суперпользователь (полный доступ к системе)

### 🎯 Основные эндпоинты

#### Задачи (`/tasks`)
- `GET /tasks` — Получить все задачи
- `POST /tasks` — Создать задачу
- `GET /tasks/{id}` — Получить задачу по ID
- `PATCH /tasks/{id}` — Обновить задачу
- `DELETE /tasks/{id}` — Удалить задачу

#### Категории (`/categories`)
- `GET /categories` — Получить все категории
- `POST /categories` — Создать категорию
- `PATCH /categories/{id}` — Обновить категорию
- `DELETE /categories/{id}` — Удалить категорию

#### Теги (`/tags`)
- `GET /tags` — Получить все теги
- `POST /tags` — Создать тег
- `PATCH /tags/{id}` — Обновить тег
- `DELETE /tags/{id}` — Удалить тег

#### Медиа-файлы (`/media`)
- `POST /media/upload/{owner_type}/{owner_id}` — Загрузить файл
- `GET /media/{file_id}/url` — Получить ссылку на файл
- `DELETE /media/{file_id}` — Удалить файл

#### Пользователи (`/users`)
- `GET /users/me` — Получить профиль текущего пользователя
- `PATCH /users/me` — Обновить профиль
- `POST /users/reset_password` — Запрос на сброс пароля
- `POST /users/check_recovery_code` — Проверка кода восстановления
- `PATCH /users/confirm_reset_password` — Подтверждение сброса пароля

#### Аутентификация (`/auth`)
- `POST /auth/register` — Регистрация пользователя
- `POST /auth/login` — Вход в систему
- `GET /auth/oauth/yandex` — OAuth вход через Yandex
- `GET /auth/oauth/yandex/callback` — OAuth callback

### 📄 Подробная документация

Полная интерактивная документация API доступна по адресу:
- **Swagger UI**: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)
- **ReDoc**: [http://127.0.0.1:8000/redoc](http://127.0.0.1:8000/redoc)
- **OpenAPI JSON**: [http://127.0.0.1:8000/openapi.json](http://127.0.0.1:8000/openapi.json)

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```bash
# База данных PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pomodoro
DB_USERNAME=pomodoro_user
DB_PASSWORD=your_secure_password

# Redis для кэширования
CACHE_HOST=localhost
CACHE_PORT=6379

# JWT токены
JWT_SECRET_KEY=your_super_secret_jwt_key_here
JWT_ALGORITHM=HS256

# MinIO/S3 для хранения файлов
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minio_access_key
S3_SECRET_KEY=minio_secret_key
S3_BUCKET=pomodoro-files

# Email настройки (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# Настройки изображений
MIN_IMAGE_SIZE=100
SMALL_SIZE=300
THUMB_SIZE=150
RATIO=1.0

# OAuth Yandex (опционально)
YANDEX_CLIENT_ID=your_yandex_client_id
YANDEX_CLIENT_SECRET=your_yandex_client_secret
```

### 🔧 Дополнительные настройки

- **Логирование**: Настроено в `pomodoro/main.py`
- **Rate Limiting**: 5 запросов в минуту по умолчанию
- **CORS**: Настроен для всех origins в разработке
- **Миграции**: Используется Alembic для управления схемой БД

## 🛠️ Разработка

### 📁 Структура проекта

```
pomodoro/
├── alembic/                # Миграции базы данных
├── pomodoro/               # Основной код приложения
│   ├── main.py            # Точка входа FastAPI
│   ├── core/              # Общие компоненты
│   ├── auth/              # Аутентификация
│   ├── user/              # Пользователи
│   ├── task/              # Задачи
│   ├── media/             # Медиа-файлы
│   └── database/          # Доступ к данным
├── scripts/               # Скрипты для обслуживания
├── tests/                 # Тесты (пока пустые)
├── docker-compose.yml     # Конфигурация инфраструктуры
├── pyproject.toml         # Зависимости и настройки Poetry
├── Makefile               # Команды для разработки
└── README.md             # Эта документация
```

### 🏃‍♂️ Команды Makefile

```bash
make run              # Запуск приложения
make migrate          # Применение миграций БД
make migrations        # Создание новой миграции
make root             # Создание суперпользователя
make e2e              # Запуск E2E тестов
make ruff             # Линтинг и форматирование
make structure        # Сохранение структуры проекта
make help             # Показать все команды
```

### 🔍 Линтинг и качество кода

Проект использует **Ruff** для линтинга и форматирования:

```bash
# Проверка и автоматическое исправление
make ruff

# Только проверка без исправлений
poetry run ruff check .

# Форматирование кода
poetry run ruff format .
```

**Стандарты кода:**
- **Длина строки**: 79 символов
- **Docstring**: Google стиль
- **Импорты**: Сортировка через isort
- **Типы**: Полная типизация с mypy

### 🗄️ Работа с базой данных

#### Миграции

```bash
# Создание миграции
make migrations MESSAGE="add user avatar field"

# Применение миграций
make migrate

# Откат миграции
alembic downgrade -1
```

#### Схема базы данных

```sql
-- Основные таблицы
users (id, phone, email, hashed_password, is_active, role, created_at, updated_at)
tasks (id, name, pomodoro_count, category_id, author_id, created_at, updated_at)
categories (id, name, author_id, created_at, updated_at)
tags (id, name, author_id, created_at, updated_at)
files (id, key, filename, size, mime_type, owner_type, owner_id, created_at)

-- Связи многие-ко-многим
task_tag (task_id, tag_id)  -- Задачи ↔ Теги
```

### 🐳 Docker для разработки

```bash
# Запуск всей инфраструктуры
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Очистка volumes
docker-compose down -v
```

**Сервисы:**
- **PostgreSQL 17**: Основная база данных
- **Redis 7**: Кэширование и сессии
- **MinIO**: S3-совместимое хранилище файлов

## 🔒 Безопасность

### 🛡️ Меры безопасности

- **JWT токены** с истечением срока действия
- **Парольный хэшинг** через Argon2
- **Rate limiting** через Redis
- **Валидация входных данных** через Pydantic
- **SQL инъекции**: Защита через SQLAlchemy ORM
- **CORS**: Настроен для доверенных origins
- **HTTPS**: Рекомендуется в продакшене

### 🔐 Аутентификация и авторизация

- **JWT Bearer tokens** для API доступа
- **OAuth 2.0** интеграция с Yandex
- **Ролевая модель** (USER, ADMIN, ROOT)
- **Восстановление пароля** через email
- **Защита от brute force** через rate limiting

## 📊 Мониторинг и логирование

### 📝 Логирование

Приложение использует структурированное логирование:

```python
import logging

logger = logging.getLogger(__name__)
logger.info("User %s created task %s", user_id, task_id)
```

**Уровни логирования:**
- **DEBUG**: Детальная информация для разработки
- **INFO**: Общая информация о работе приложения
- **WARNING**: Предупреждения о потенциальных проблемах
- **ERROR**: Ошибки выполнения
- **CRITICAL**: Критические ошибки

### 📈 Метрики

- **FastAPI metrics**: Автоматически собираются через middleware
- **Database queries**: Логирование медленных запросов
- **Cache hit/miss ratio**: Статистика Redis
- **Rate limiting**: Мониторинг превышений лимитов

## 🚀 Развертывание

### 🏭 Продакшн развертывание

1. **Настройте сервер** (Ubuntu 22.04+ рекомендуется)
2. **Установите Docker и Docker Compose**
3. **Клонируйте репозиторий**
4. **Настройте переменные окружения** для продакшена
5. **Запустите через docker-compose.prod.yml** (если есть)
6. **Настройте reverse proxy** (nginx/caddy)
7. **Включите HTTPS** через Let's Encrypt
8. **Настройте мониторинг** (Prometheus + Grafana)

### 🔧 Переменные продакшена

```bash
# Используйте сильные пароли
DB_PASSWORD=very_secure_db_password
JWT_SECRET_KEY=256_bit_random_secret_key
S3_SECRET_KEY=strong_minio_secret

# Настройки для продакшена
ENVIRONMENT=production
LOG_LEVEL=INFO

# Домены и CORS
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
```

## 🤝 Вклад в проект

### 📋 Процесс разработки

1. **Создайте issue** с описанием задачи
2. **Создайте feature branch**: `git checkout -b feature/your-feature`
3. **Напишите код** с тестами
4. **Запустите линтинг**: `make ruff`
5. **Создайте pull request**
6. **Пройдите code review**

### 📏 Стандарты кода

- **PEP 8** для Python кода
- **Google style** для docstring
- **Типизация** обязательна для всех функций
- **Тесты** для новой функциональности
- **Документация** API через docstring

### 🧪 Тестирование

```bash
# Запуск E2E тестов
make e2e

# Добавление новых тестов в scripts/e2e.py
# Тесты должны проверять основные пользовательские сценарии
```

## 📄 Лицензия

Этот проект лицензирован под **MIT License** - см. файл [LICENSE](LICENSE) для деталей.

## 👥 Авторы

- **Alexandr Novoseletsky** - *Основной разработчик* - [GitHub](https://github.com/AleexandrNovoseletsky)

## 🙏 Благодарности

- **FastAPI** за отличный веб-фреймворк
- **SQLAlchemy** за мощный ORM
- **Pydantic** за валидацию данных
- **Poetry** за управление зависимостями
- **Docker** за контейнеризацию

---

**⭐ Если проект оказался полезным, поставьте звезду на GitHub!**

[📧 Связаться с разработчиком](mailto:night2324@yandex.ru) | [🐛 Сообщить об ошибке](https://github.com/AleexandrNovoseletsky/pomodoro/issues)